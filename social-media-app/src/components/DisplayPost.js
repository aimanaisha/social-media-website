
const DisplayPost = (props) => {

        return(
            <div>
                {/* {props.post.text && <p>{props.post.text}</p>}
                {props.post.file && <img src={props.post.file}/>} */}
                
            </div>
        )
} 

export default DisplayPost